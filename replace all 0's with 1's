#include<stdio.h>
#include<math.h>
int main()
{
int n=102301;
int ans=0;
int i=0;
while(n!=0){
if(n%10==0)
ans=ans+1*pow(10,i);
else
ans=ans+(n%10)*pow(10,i);
n=n/10;
i++;
}
printf("%d",ans);
return 0;
}

